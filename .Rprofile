options(ed_sql_path = normalizePath(file.path("db", "eidith.sqlite"), mustWork = FALSE))
options(ed_temp_sql_path = normalizePath(file.path("temp", "temp_db.sqlite"), mustWork = FALSE))
options(echo=TRUE)

